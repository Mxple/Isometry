// constants

// change these lines
let rows = 64;
let cols = 196;
let layers = 1;
export const TSIZE = 48;

export const OFFSETS = {
	xOffset : 0,
	yOffset : 0
}

// do not change stuff under here
export const DIM = {
	ROWS : rows,
	COLS : cols,
	LAYS : layers,
	LAYER_SIZE : rows * cols,
	TOTAL_SIZE : rows * cols * layers
}
